# 改动说明（大白话版）

这套代码 = 老师的"电机控制骨架" + 我补的"缺件和修错"。
下面每条改动都配了一句大白话，解释「是什么」和「为什么」。

---

## 一、删掉的文件

- **`Common/common.c`**
  老师留的空文件，里面一个字都没有，没用，删了。它本来该放的"工具函数"，我合并到 `dj_motor.c` 里了。

## 二、新写的文件（原来是空的）

- **`Common/macro.h`**
  放了一些"小工具"宏：`ABS`(取绝对值)、`GetSign`(取正负号)、`Clamp`(把数限制在一个范围内)。这些是代码里到处要用的小零件，单独放一个文件方便复用。

- **`Drivers/pid.h`**
  定义了 PID 的"数据盒子"（`KP/KI/KD` 参数 + 误差记录），以及三个函数。PID 是控制算法，用来算"该给电机多大的力"。

- **`README.md`**
  操作手册：CubeMX 怎么点、Keil 怎么加文件、电机怎么接线、怎么一步步安全测试。

## 三、改了哪些文件、每处是什么意思

### 1. `Drivers/pid.c`（老师原来只有 `PID_Caculate` 一个函数）

- **加 `#include "pid.h"`**
  引入 PID 的定义，否则编译不知道 `PIDType` 是啥。

- **加 `PID_Init()`**
  初始化 PID：把 `KP/KI/KD` 参数填进去、清空历史。相当于"开机重置"。

- **加 `PID_Reset()`**
  清空 PID 的历史误差和输出，让 PID 从头开始算。切换模式时用它，避免旧数据捣乱。

### 2. `Drivers/dj_motor.h`（头文件）

- **重排类型定义顺序**
  老师把一些"盒子"（如 `DJmotorVal`）的定义写在后面，但前面就用到了。C 语言不认"先用后定义"，所以我把所有盒子的定义都挪到前面。**纯编译需要，逻辑没变。**

- **补结构体 `DJmotorStatus / DJmotorArgum / DJmotorError`**
  老师代码里用了这三个"盒子"，但没给出盒子的定义（应该是漏了），我补上：状态标志、参数、错误计数。

- **加 `DJmotorRxPacket` + 队列声明**
  "队列"就是排队：CAN 收到的数据先排队，定时器再一个一个取出来处理。这里定义了排队的"格子"和"排队/取队"两个操作。

### 3. `Drivers/dj_motor.c`（核心，改动最多）

**新增的部分（文件开头 1~76 行）：**

- **`#include` + `DJmotor` 全局数组**
  真正创建 8 台电机的数据存储。

- **`DJmotor_GetCanHandle()`**
  返回 CAN 外设的"身份证"，发数据时要用它。

- **`DJmotor_SetZero()`**
  设零点：把电机当前的位置当成"0 度"。（老师给的这个实现）

- **`DJmotor_PID_Reload()`**
  重新加载 PID 参数。

- **`EncodeS16Data()` / `ChangeDataByte()`**
  把电流值拆成两个字节，并交换字节顺序（大端→小端转换）。大疆电机要求这种字节序，不做转换数据会乱。

- **5 个函数的前置声明**
  告诉编译器"这几个函数在后面定义"，防止"先用后定义"报错。

- **软件队列**
  排队缓冲区 + 两个操作：`DJmotor_RxPush`（排队）、`DJmotor_RxProcess`（取队并解包）。

**修改老师原有代码的 8 处：**

1. **`DJmotor_Init` 两个 for 循环对调**
   老师把 M2006 和 M3508 的顺序写反了，导致发控制帧时地址对不上（M3508 该发 `0x200` 却发成 `0x1FF`）。调换后：M3508 占 ID1~4、M2006 占 ID5~8。

2. **`DJmotor_Receive` 删掉 `speed_rpm /= (减速比)`**
   原来把转速除以减速比，但 `speed_rpm` 是整数，除法会截断、丢精度。删掉后保留原始的高精度转速。

3. **启用 `DJmotor_Monitor`**
   这是"安全保护"：通信断了自动断电、堵转自动停。老师写了但被注释掉了，我打开它，防止烧电机。

4. **`DJ_Current` 限幅写回**
   原来限幅算出来了但没存回去（等于没限幅）。补上，防止电流超限烧电调。

5. **`DJmotor_SwitchMode` 切位置模式时设零点**
   切到位置模式时，先把当前位置设成 0 度，防止电机猛回跳（这就是老师说的"跑完速度再跑位置要设零点"）。

6. **`DJmotor_SpeedMode` 的 `CurVal` 直接取**
   转速直接用（不再乘减速比），配合第 2 条，避免精度丢失。

7. **`DJmotor_PositionMode` 的 `CurVal` 直接取**
   同上。

8. **`DJmotor_Monitor` 堵转判断加 `ABS`**
   判断"没转"要取绝对值（正转反转都可能卡死），原来只判正方向，会误判导致电机突然停。

### 4. `H7_DJ_motor/Core/Src/main.c`（CubeMX 生成的，只改"用户代码区"）

- **加 `#include "dj_motor.h"`**
  让 main 能用电机驱动。

- **加 CAN 过滤器 + 启动 CAN + 打开 CAN 中断 + 初始化电机 + 启动定时器**
  开机时把这套系统跑起来：只收电机的反馈帧、启动 CAN、初始化 8 台电机、启动 1ms 定时器。

- **CAN 接收回调改成"塞队列"**
  CAN 收到数据不再直接处理，先排队。

- **加 TIM2 中断回调**
  定时器每 1ms 触发一次：从队列取数据解析，再跑控制。

- **主循环清空**
  控制都搬到定时器里了，主循环空着。

## 四、整体流程（大白话）

1. 电机每隔一段时间发一帧反馈（角度/转速/电流）。
2. CAN 收到后，先塞进"队列"排队。
3. 定时器每 1ms 响一次：从队列取数据解析（`DJmotor_Receive`），再跑状态机算 PID（`DJmotor_Func`），最后发控制指令。
4. 电机收到指令转动，再发新反馈，循环。

